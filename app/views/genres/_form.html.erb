<%= form_with(model: @genre, html: { multipart: true }, local: true) do |f| %>
  <p><%= f.label :name %></p>
  <p><%= f.text_field :name %></p>

  <p><%= f.submit class:"btn btn-light" %></p>
<% end %>