<%= form_with(model: @user, html: { multipart: true }, local: true) do |f| %>
  <p><%= f.label :full_name %></p>
  <p><%= f.text_field :full_name %></p>

  <p><%= f.label :birth_date %></p>
  <p><%= f.text_field :birth_date%></p>

  <p><%= f.submit %></p>
<% end %>